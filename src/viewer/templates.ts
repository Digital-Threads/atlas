export const viewerHtml = `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Atlas Architecture Map</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header class="topbar">
    <div class="identity"><button id="back" class="icon-button" title="Back" disabled>←</button><strong>Atlas</strong><span id="project-name">Architecture Map</span></div>
    <div class="toolbar"><button id="fit" title="Fit graph">Fit</button><button id="zoom-in" class="icon-button" title="Zoom in">+</button><button id="zoom-out" class="icon-button" title="Zoom out">−</button></div>
  </header>
  <main class="workspace">
    <aside class="sidebar">
      <label for="search">Search</label>
      <input id="search" type="search" placeholder="Service, route, file..." autocomplete="off">
      <div id="search-results" class="search-results" hidden></div>
      <h2>Modes</h2><div id="modes" class="stack"></div>
      <h2>Filters</h2><div id="filters" class="filters"></div>
      <h2>Project</h2><dl id="stats" class="stats"></dl>
    </aside>
    <section class="canvas-wrap"><div id="cy"></div><div id="empty" hidden>No nodes match the current view.</div><div id="tooltip" hidden></div></section>
    <aside id="details" class="details closed">
      <button id="details-close" class="icon-button close-button" title="Close details">×</button>
      <div id="details-empty">Select a node to inspect it.</div>
      <div id="details-content" hidden></div>
    </aside>
  </main>
  <script src="cytoscape.min.js"></script>
  <script src="graph-data.js"></script>
  <script src="app.js"></script>
</body>
</html>`;

export const viewerCss = `:root{--ink:#18211f;--muted:#66716d;--paper:#f7f4ec;--panel:#fffdfa;--line:#d8d1c2;--dark:#202826;--accent:#d9692d}*{box-sizing:border-box}html,body{height:100%;margin:0}body{font:14px/1.45 Arial,sans-serif;color:var(--ink);background:var(--paper);overflow:hidden}.topbar{height:58px;display:flex;align-items:center;justify-content:space-between;padding:0 16px;background:var(--dark);color:#fff7e6}.identity,.toolbar,.actions,.tabs{display:flex;align-items:center;gap:6px}.identity strong{font-size:18px;margin-left:4px}.identity span{color:#bfc8c4;margin-left:6px}.topbar button,.details button,.details a.command{border:1px solid #53605b;background:#2a3431;color:#fff;border-radius:5px;min-height:34px;padding:7px 11px;cursor:pointer;text-decoration:none;font:inherit}.icon-button{width:36px;padding:0!important;font-size:18px}.topbar button:disabled{opacity:.4;cursor:default}.workspace{height:calc(100% - 58px);display:grid;grid-template-columns:230px minmax(320px,1fr) 330px}.sidebar,.details{overflow:auto;padding:16px;background:#f2eee3}.sidebar{border-right:1px solid var(--line)}.details{position:relative;border-left:1px solid var(--line);background:var(--panel)}label,h2{display:block;font-size:11px;text-transform:uppercase;font-weight:800;color:var(--muted);margin:0 0 8px;letter-spacing:0}input{width:100%;height:38px;padding:0 10px;border:1px solid var(--line);border-radius:5px;background:#fff;margin-bottom:12px}.stack{display:grid;gap:5px;margin-bottom:20px}.stack button{border:1px solid transparent;background:transparent;text-align:left;padding:8px;border-radius:5px;cursor:pointer}.stack button.active{border-color:#e8b28f;background:#fbe8d9;color:#8e3e17;font-weight:700}.filters{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:20px}.filters label{display:flex;align-items:center;gap:5px;text-transform:none;font-size:12px;margin:0;padding:6px;background:#fff;border:1px solid var(--line);border-radius:5px;overflow-wrap:anywhere}.filters input{width:auto;height:auto;margin:0}.stats{display:grid;grid-template-columns:1fr auto;gap:6px}.stats dt{color:var(--muted)}.stats dd{margin:0;font-weight:700;max-width:115px;text-align:right;overflow-wrap:anywhere}.search-results{display:grid;gap:4px;max-height:240px;overflow:auto;margin:0 0 16px;padding:6px;border:1px solid var(--line);background:#fff}.search-results[hidden]{display:none}.search-results button{display:grid;gap:1px;border:0;background:#fff;text-align:left;padding:6px;cursor:pointer}.search-results button:hover{background:#f2eee3}.search-results small{color:var(--muted);overflow-wrap:anywhere}.canvas-wrap{position:relative;background:#fffcf4}.canvas-wrap:before{content:"";position:absolute;inset:0;background-image:radial-gradient(#cfc7b7 1px,transparent 1px);background-size:24px 24px;opacity:.6}#cy{position:absolute;inset:0}#empty{position:absolute;inset:0;display:grid;place-items:center;color:var(--muted);pointer-events:none}#empty[hidden]{display:none}#tooltip{position:fixed;z-index:8;max-width:310px;padding:10px;background:#17201e;color:#fff;border-radius:5px;box-shadow:0 8px 30px #0004;pointer-events:none;font-size:12px}#tooltip strong{display:block;font-size:14px}.tooltip-purpose{margin:5px 0;color:#d7dfdb}.badge{display:inline-block;padding:4px 7px;border-radius:999px;background:#eee7dc;font-size:11px;font-weight:800}.details h1{font-size:21px;line-height:1.15;margin:10px 38px 5px 0;overflow-wrap:anywhere}.details h2{margin-top:20px}.path{font:12px/1.4 monospace;color:var(--muted);overflow-wrap:anywhere}.purpose{margin:12px 0;color:#36413e}.detail-grid{display:grid;grid-template-columns:1fr auto;gap:7px;border-block:1px solid var(--line);padding:12px 0;margin:14px 0}.detail-grid span:nth-child(odd){color:var(--muted)}.actions{flex-wrap:wrap}.tabs{overflow-x:auto;margin:16px 0 12px;border-bottom:1px solid var(--line)}.tabs button{border:0;background:transparent;color:var(--muted);border-radius:0;padding:7px 5px;min-height:32px}.tabs button.active{color:var(--ink);border-bottom:2px solid var(--accent)}.tab-panel[hidden]{display:none}.relations{display:grid;gap:7px}.relation{padding:8px;border:1px solid var(--line);border-radius:5px;background:#f8f5ed;cursor:pointer;overflow-wrap:anywhere}.relation small{display:block;color:var(--muted)}.source{white-space:pre-wrap;overflow:auto;max-height:330px;padding:10px;background:#17201e;color:#eae4d8;border-radius:5px;font:11px/1.5 monospace}.notice{padding:10px;background:#fff2d7;border:1px solid #e6c678;border-radius:5px}.close-button{position:absolute;right:12px;top:12px;display:none}.group-count{color:var(--muted);font-weight:400}@media(max-width:900px){.workspace{grid-template-columns:190px minmax(0,1fr)}.filters{grid-template-columns:1fr}.details{position:absolute;right:0;top:58px;bottom:0;width:min(360px,92vw);z-index:7;box-shadow:-12px 0 40px #0002}.details.closed{display:none}.close-button{display:block}}@media(max-width:560px){.workspace{grid-template-columns:150px minmax(0,1fr)}.sidebar{padding:12px}.identity span,#zoom-in,#zoom-out{display:none}.topbar{padding:0 8px}}`;

export const viewerJs = `
(function () {
  var graph = window.__ATLAS_GRAPH__;
  if (!graph || !Array.isArray(graph.nodes) || !Array.isArray(graph.edges)) throw new Error('Atlas graph data is missing or invalid.');
  var nodeMap = new Map(graph.nodes.map(function(node){return [node.id,node];}));
  var types = Array.from(new Set(graph.nodes.map(function(node){return node.type;}))).sort();
  var colors = {route:'#e16f2d',controller:'#2a6fbb',service:'#7a4ec2',provider:'#8158a8',repository:'#4f63a8',database:'#278463',table:'#278463',model:'#278463',entity:'#2f8d70',column:'#63a88d',dto:'#197d8d',external_api:'#c94646',environment_variable:'#b28412',test:'#777',module:'#c6528e',risk:'#d12f2f',method:'#53605b',function:'#53605b',file:'#303735',guard:'#405b57',pipe:'#197d8d',interceptor:'#6f4c84',middleware:'#6d6351'};
  var elements = graph.nodes.map(function(node){return {data:Object.assign({},node,{color:colors[node.type]||'#59645f'})};}).concat(graph.edges.map(function(edge){return {data:Object.assign({},edge,{source:edge.from,target:edge.to,analysisSource:edge.source})};}));
  var cy = cytoscape({container:document.getElementById('cy'),elements:elements,minZoom:.05,maxZoom:3,
    style:[
      {selector:'node',style:{'background-color':'data(color)',label:'data(label)','font-size':10,'text-wrap':'wrap','text-max-width':110,color:'#18211f','text-valign':'bottom','text-margin-y':7,width:22,height:22,'border-width':2,'border-color':'#fff'}},
      {selector:'node:selected',style:{'border-width':4,'border-color':'#18211f',width:30,height:30}},
      {selector:'edge',style:{width:1.2,'line-color':'#a8aaa5','target-arrow-color':'#a8aaa5','target-arrow-shape':'triangle','curve-style':'bezier','arrow-scale':.7,opacity:.7}},
      {selector:'.faded',style:{opacity:.08}},{selector:'.highlight',style:{'line-color':'#e16f2d','target-arrow-color':'#e16f2d',width:3,opacity:1}}
    ],layout:{name:'preset'}});
  var modes={Overview:['project','module','controller','service','provider','repository','database','model','table','entity','external_api','risk'],Modules:['project','module','controller','service','provider','repository','dto','guard','pipe','interceptor','middleware'],Routes:['route','controller','method','service','table','external_api','dto','guard','pipe','interceptor'],Services:['service','provider','repository','method','table','external_api','environment_variable','test'],Dependencies:null,Database:['database','model','table','column','entity','repository','service','method'],External:['external_api','environment_variable','service','method'],Risks:['risk','service','controller','route','file'],Code:['project','folder','file','package','library','module','controller','service','provider','repository','dto','decorator','test','function']};
  var activeMode='Overview',selectedTypes=new Set(types),selectedId=null,pinnedId=null,history=[];
  var modesEl=document.getElementById('modes'),filters=document.getElementById('filters'),search=document.getElementById('search'),results=document.getElementById('search-results');
  Object.keys(modes).forEach(function(name){var button=document.createElement('button');button.textContent=name;button.className=name===activeMode?'active':'';button.onclick=function(){activeMode=name;pinnedId=null;updateModeButtons();applyView(true);};modesEl.appendChild(button);});
  types.forEach(function(type){var label=document.createElement('label'),input=document.createElement('input');input.type='checkbox';input.checked=true;input.onchange=function(){input.checked?selectedTypes.add(type):selectedTypes.delete(type);applyView(true);};label.appendChild(input);label.appendChild(document.createTextNode(type.replaceAll('_',' ')));filters.appendChild(label);});
  renderStats();
  document.getElementById('project-name').textContent=graph.project.name;
  search.oninput=function(){renderSearchResults();applyView(true);};
  function renderStats(){var stats=document.getElementById('stats');var pairs=[['Stack',graph.project.detectedStacks.join(', ')||'none'],['Files',count('file')],['Nodes',graph.stats.totalNodes],['Edges',graph.stats.totalEdges],['Routes',count('route')],['Controllers',count('controller')],['Services',count('service')],['Tables',count('table')],['External APIs',count('external_api')],['Risks',count('risk')]];pairs.forEach(function(pair){var dt=document.createElement('dt'),dd=document.createElement('dd');dt.textContent=pair[0];dd.textContent=pair[1];stats.append(dt,dd);});}
  function count(type){return graph.stats.byNodeType[type]||0;}
  function updateModeButtons(){Array.from(modesEl.children).forEach(function(button){button.classList.toggle('active',button.textContent===activeMode);});}
  function renderSearchResults(){var needle=search.value.trim().toLowerCase();results.innerHTML='';results.hidden=!needle;if(!needle)return;var found=graph.nodes.filter(function(node){return searchable(node).includes(needle);}).sort(function(a,b){return searchScore(b,needle)-searchScore(a,needle);}).slice(0,30);if(!found.length){results.innerHTML='<div class="notice">No results.</div>';return;}found.forEach(function(node){var button=document.createElement('button');button.innerHTML='<strong>'+escapeHtml(node.label)+'</strong><small>'+escapeHtml(node.type+(node.file?' · '+node.file:''))+'</small>';button.onclick=function(){selectNode(node.id,true);};results.appendChild(button);});}
  function searchable(node){return [node.id,node.label,node.name,node.type,node.file,JSON.stringify(node.metadata||{})].filter(Boolean).join(' ').toLowerCase();}
  function searchScore(node,needle){var label=node.label.toLowerCase();return label===needle?100:label.startsWith(needle)?80:node.id.toLowerCase().includes(needle)?60:20;}
  function applyView(fit){var needle=search.value.trim().toLowerCase(),allowed=needle?null:modes[activeMode];cy.batch(function(){cy.elements().show();cy.nodes().forEach(function(node){var data=node.data();var hide=!selectedTypes.has(data.type)||(allowed&&!allowed.includes(data.type))||(needle&&!searchable(data).includes(needle));if(data.id===pinnedId)hide=false;if(hide)node.hide();});cy.edges().forEach(function(edge){if(edge.source().hidden()||edge.target().hidden())edge.hide();});});var visibleNodes=cy.nodes(':visible');document.getElementById('empty').hidden=visibleNodes.length>0;if(!visibleNodes.length)return;layoutVisible(visibleNodes,fit);}
  function layoutVisible(visibleNodes,fit){var visible=cy.elements(':visible');if(visibleNodes.length>1000){cy.layout({name:'grid',eles:visible,fit:fit,padding:55,avoidOverlap:true}).run();return;}var columns=[[],[],[],[],[],[]];visibleNodes.forEach(function(node){columns[nodeLayer(node.data())].push(node);});columns.forEach(function(column){column.sort(function(a,b){return a.data('label').localeCompare(b.data('label'));});});var largest=Math.max.apply(null,columns.map(function(column){return column.length;})),positions={};columns.forEach(function(column,columnIndex){var offset=(largest-column.length)*45;column.forEach(function(node,index){positions[node.id()]={x:100+columnIndex*190,y:70+offset+index*90};});});cy.layout({name:'preset',eles:visible,positions:positions,fit:fit,padding:65,animate:false}).run();}
  function nodeLayer(data){if(['project','folder','file','package','library','config','module'].includes(data.type))return 0;if(['route','controller'].includes(data.type))return 1;if(data.type==='method'){var owner=String(data.metadata&&data.metadata.class||data.label).toLowerCase();return owner.includes('controller')?2:3;}if(['dto','guard','pipe','interceptor','middleware'].includes(data.type))return 2;if(['service','provider','repository','function'].includes(data.type))return 3;if(['database','model','table','column','entity','external_api','environment_variable'].includes(data.type))return 4;return 5;}
  function clearHighlight(){cy.elements().removeClass('faded highlight');}
  function traverse(id,direction,depth,allowed){var nodeIds=new Set([id]),edgeIds=new Set(),front=[id];for(var level=0;level<depth&&front.length;level+=1){var next=[];front.forEach(function(nodeId){graph.edges.filter(function(edge){var directional=direction==='out'?edge.from===nodeId:direction==='in'?edge.to===nodeId:edge.from===nodeId||edge.to===nodeId;return directional&&(!allowed||allowed.has(edge.type));}).forEach(function(edge){edgeIds.add(edge.id);var neighbor=edge.from===nodeId?edge.to:edge.from;if(!nodeIds.has(neighbor)){nodeIds.add(neighbor);next.push(neighbor);}});});front=next;}return {nodeIds:nodeIds,edgeIds:edgeIds};}
  function highlight(subgraph,fit){clearHighlight();cy.elements().addClass('faded');var nodes=cy.collection(Array.from(subgraph.nodeIds).map(function(id){return cy.getElementById(id);})),edges=cy.collection(Array.from(subgraph.edgeIds).map(function(id){return cy.getElementById(id);}));nodes.show().removeClass('faded');edges.show().removeClass('faded').addClass('highlight');if(fit)cy.fit(nodes.union(edges),65);}
  function findFlow(id){var route=nodeMap.get(id);if(!route||route.type!=='route'){var incoming=traverse(id,'in',10,new Set(['handles','calls','reads','writes','uses','connects_to','validates','returns']));route=Array.from(incoming.nodeIds).map(function(nodeId){return nodeMap.get(nodeId);}).find(function(node){return node&&node.type==='route';});}return route?{root:route,subgraph:traverse(route.id,'out',12,new Set(['handles','calls','reads','writes','uses','connects_to','validates','returns']))}:null;}
  function selectNode(id,record){var data=nodeMap.get(id);if(!data)return;if(record&&selectedId&&selectedId!==id)history.push(selectedId);selectedId=id;pinnedId=id;document.getElementById('back').disabled=history.length===0;applyView(false);var node=cy.getElementById(id);node.select();highlight(traverse(id,'both',1),false);showDetails(data);document.getElementById('details').classList.remove('closed');}
  cy.on('tap','node',function(event){selectNode(event.target.id(),true);});
  cy.on('mouseover','node',function(event){var data=event.target.data(),outgoing=graph.edges.filter(function(edge){return edge.from===data.id;}).slice(0,4).map(function(edge){return nodeMap.get(edge.to);}).filter(Boolean).map(function(node){return node.label;}),tooltip=document.getElementById('tooltip');tooltip.innerHTML='<strong>'+escapeHtml(data.label)+'</strong><div>'+escapeHtml(data.type+(data.framework?' · '+data.framework:''))+'</div><div class="tooltip-purpose">Purpose: '+escapeHtml(data.metadata&&data.metadata.description||'Not detected yet')+'</div><div>'+escapeHtml(data.file||'No file')+'</div><div>Incoming: '+event.target.incomers('edge').length+' · Outgoing: '+event.target.outgoers('edge').length+'</div><div>Dependencies: '+escapeHtml(outgoing.join(', ')||'none detected')+'</div>';tooltip.hidden=false;});
  cy.on('mousemove','node',function(event){var tooltip=document.getElementById('tooltip');tooltip.style.left=(event.originalEvent.clientX+14)+'px';tooltip.style.top=(event.originalEvent.clientY+14)+'px';});cy.on('mouseout','node',function(){document.getElementById('tooltip').hidden=true;});
  function showDetails(data){var panel=document.getElementById('details-content');document.getElementById('details-empty').hidden=true;panel.hidden=false;var incoming=graph.edges.filter(function(edge){return edge.to===data.id;}),outgoing=graph.edges.filter(function(edge){return edge.from===data.id;}),related=traverse(data.id,'both',8),relatedNodes=Array.from(related.nodeIds).map(function(id){return nodeMap.get(id);}).filter(Boolean).filter(function(node){return node.id!==data.id;}),source=data.metadata&&data.metadata.sourcePreview||'',loc=data.sourceLocation||{},flow=findFlow(data.id);panel.innerHTML='<span class="badge">'+escapeHtml(data.type)+'</span><h1>'+escapeHtml(data.label)+'</h1><div class="path">'+escapeHtml(data.file||'No source file')+(loc.startLine?' : '+loc.startLine+'-'+(loc.endLine||loc.startLine):'')+'</div><p class="purpose">'+escapeHtml(data.metadata&&data.metadata.description||'Purpose: Not detected yet')+'</p><div class="detail-grid"><span>Framework</span><b>'+escapeHtml(data.framework||'—')+'</b><span>Language</span><b>'+escapeHtml(data.language||'—')+'</b><span>Incoming</span><b>'+incoming.length+'</b><span>Outgoing</span><b>'+outgoing.length+'</b><span>Confidence</span><b>'+(data.confidence==null?'—':data.confidence)+'</b><span>Source</span><b>'+escapeHtml(data.source||'—')+'</b></div><div class="actions"><button id="focus-node">Focus</button><button id="show-dependencies">Dependencies</button><button id="show-dependents">Dependents</button><button id="show-flow">Flow</button><button id="module-drill">Module</button><button id="copy-id">Copy ID</button></div><div class="tabs"><button class="active" data-tab="overview">Overview</button><button data-tab="incoming">Incoming</button><button data-tab="outgoing">Outgoing</button><button data-tab="source">Source</button><button data-tab="flow">Flow</button></div><section class="tab-panel" data-panel="overview">'+detailGroups(relatedNodes)+'</section><section class="tab-panel" data-panel="incoming" hidden>'+relationList(incoming,data.id)+'</section><section class="tab-panel" data-panel="outgoing" hidden>'+relationList(outgoing,data.id)+'</section><section class="tab-panel" data-panel="source" hidden>'+(source?'<div class="actions"><a id="open-source" class="command" href="'+escapeHtml(sourceUrl(data.file))+'" target="_blank">Open source</a></div><pre class="source">'+escapeHtml(source)+'</pre>':'<div class="notice">Source preview is not available.</div>')+'</section><section class="tab-panel" data-panel="flow" hidden>'+(flow?flowList(flow.subgraph):'<div class="notice">No HTTP route flow reaches this node.</div>')+'</section>';bindDetails(data,flow);}
  function detailGroups(nodes){var groups=[['Methods',['method','function']],['Routes',['route']],['Database',['database','model','table','column','entity','repository']],['Environment',['environment_variable']],['External APIs',['external_api']],['Tests',['test']]];return groups.map(function(group){var items=nodes.filter(function(node){return group[1].includes(node.type);});return '<h2>'+group[0]+' <span class="group-count">'+items.length+'</span></h2>'+nodeList(items);}).join('');}
  function nodeList(nodes){if(!nodes.length)return '<div class="notice">None detected.</div>';return '<div class="relations">'+nodes.slice(0,100).map(function(node){return '<div class="relation node-link" data-id="'+escapeHtml(node.id)+'"><strong>'+escapeHtml(node.label)+'</strong><small>'+escapeHtml(node.type+(node.file?' · '+node.file:''))+'</small></div>';}).join('')+'</div>';}
  function relationList(edges,currentId){if(!edges.length)return '<div class="notice">No connections detected.</div>';return '<div class="relations">'+edges.slice(0,150).map(function(edge){var otherId=edge.from===currentId?edge.to:edge.from,other=nodeMap.get(otherId);return '<div class="relation node-link" data-id="'+escapeHtml(otherId)+'"><strong>'+escapeHtml(edge.type)+'</strong> '+escapeHtml(other?other.label:'Unknown')+'<small>'+escapeHtml(edge.source||'unknown source')+' · confidence '+(edge.confidence==null?'—':edge.confidence)+'</small></div>';}).join('')+'</div>';}
  function flowList(subgraph){var nodes=Array.from(subgraph.nodeIds).map(function(id){return nodeMap.get(id);}).filter(Boolean);return '<div class="relations">'+nodes.map(function(node,index){return '<div class="relation node-link" data-id="'+escapeHtml(node.id)+'"><strong>'+(index+1)+'. '+escapeHtml(node.label)+'</strong><small>'+escapeHtml(node.type)+'</small></div>';}).join('')+'</div>';}
  function bindDetails(data,flow){var panel=document.getElementById('details-content');panel.querySelectorAll('.node-link').forEach(function(item){item.onclick=function(){selectNode(item.dataset.id,true);};});panel.querySelectorAll('[data-tab]').forEach(function(button){button.onclick=function(){panel.querySelectorAll('[data-tab]').forEach(function(item){item.classList.toggle('active',item===button);});panel.querySelectorAll('[data-panel]').forEach(function(section){section.hidden=section.dataset.panel!==button.dataset.tab;});};});panel.querySelector('#focus-node').onclick=function(){cy.animate({center:{eles:cy.getElementById(data.id)},zoom:1.6},{duration:250});};panel.querySelector('#show-dependencies').onclick=function(){highlight(traverse(data.id,'out',4),true);};panel.querySelector('#show-dependents').onclick=function(){highlight(traverse(data.id,'in',4),true);};panel.querySelector('#show-flow').onclick=function(){if(flow){highlight(flow.subgraph,true);var tab=panel.querySelector('[data-tab="flow"]');if(tab)tab.click();}};panel.querySelector('#module-drill').onclick=function(){var module=data.type==='module'?data:Array.from(traverse(data.id,'in',6).nodeIds).map(function(id){return nodeMap.get(id);}).find(function(node){return node&&node.type==='module';});if(module)highlight(traverse(module.id,'both',2),true);};panel.querySelector('#copy-id').onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(data.id);};}
  function sourceUrl(file){if(!file)return '#';var separator=String.fromCharCode(92),root=String(graph.project.root||'').split(separator).join('/');if(root.endsWith('/'))root=root.slice(0,-1);var path=(root+'/'+file).split(separator).join('/');return path.charAt(1)===':'?'file:///'+encodeURI(path):'file://'+encodeURI(path);}
  document.getElementById('back').onclick=function(){var id=history.pop();document.getElementById('back').disabled=history.length===0;if(id)selectNode(id,false);};document.getElementById('details-close').onclick=function(){document.getElementById('details').classList.add('closed');};document.getElementById('fit').onclick=function(){clearHighlight();cy.fit(cy.elements(':visible'),45);};document.getElementById('zoom-in').onclick=function(){cy.zoom({level:cy.zoom()*1.2,renderedPosition:{x:cy.width()/2,y:cy.height()/2}});};document.getElementById('zoom-out').onclick=function(){cy.zoom({level:cy.zoom()/1.2,renderedPosition:{x:cy.width()/2,y:cy.height()/2}});};
  function escapeHtml(value){return String(value==null?'':value).replace(/[&<>"']/g,function(character){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[character];});}
  applyView(true);
})();`;
